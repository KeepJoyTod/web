# 修复注册接口500错误

- 会话 ID: `019ddcc8-3cf9-7432-94d5-6c5de3fdf58e`
- 开始时间: `2026-04-30T05:06:43.597Z`
- 更新时间: `2026-04-30T05:07:02.6804695Z`
- 原始文件: `C:\Users\Administrator\.codex\sessions\2026\04\30\rollout-2026-04-30T13-06-43-019ddcc8-3cf9-7432-94d5-6c5de3fdf58e.jsonl`

## 用户 · 2026-04-30T05:07:02.564Z

http://localhost:5173/api/v1/auth/register  
POST   500 Internal Server Error注册功能出错

## 用户 · 2026-04-30T05:07:12.418Z

http://localhost:5173/api/v1/auth/register  
POST   500 Internal Server Error注册功能出错

## 用户 · 2026-04-30T05:08:37.065Z

http://localhost:5173/api/v1/auth/register  
POST   500 Internal Server Error注册功能出错

## 助手 · 2026-04-30T05:08:45.168Z

这个报错我来直接排查到底。先在当前项目里复现并抓后端异常日志，定位 `register` 接口为什么返回 500，然后我会直接改掉并帮你验证。

## 助手 · 2026-04-30T05:09:06.770Z

我先改用 PowerShell 自带检索（`rg` 在这个环境里权限报错），正在定位后端 `register` 具体实现和日志里 500 的堆栈。

## 助手 · 2026-04-30T05:09:25.198Z

我已经定位到 `register` 代码了，下一步在数据库插入层和全局异常处理里看 500 的具体触发点（很可能是字段约束或空值导致的 SQL 异常），我会马上给出修复并回归验证。

## 助手 · 2026-04-30T05:09:54.929Z

我继续往前端请求参数这层查一下，因为现在看后端 `register` 没做参数校验，前端如果传的是 `username/email` 而不是 `account`，会直接触发数据库异常并返回 500。接着我会把这块一次性修好。

## 助手 · 2026-04-30T05:10:22.128Z

我抓到一个关键点：`frontend` 代理固定转发到 `127.0.0.1:8080`，而你日志里还有一个跑在 `18080` 的后端实例。现在我马上直接对 `5173` 发注册请求，拿到真实 500 响应体和后端端口状态，确认是不是请求打到了“旧服务”。

## 助手 · 2026-04-30T05:10:52.925Z

我先把请求响应体直接抓出来，同时只在前端 `src` 里查注册参数字段名。这样能很快确认是“端口错连”还是“参数名不匹配/数据库字段约束”导致的 500。

## 助手 · 2026-04-30T05:11:37.211Z

现在已经复现到真实 500，下一步我会单独拉起一个可控的后端实例（18080）并抓完整异常堆栈，这样能准确改根因，不会盲改。

## 助手 · 2026-04-30T05:11:58.728Z

我已经找到一个很关键的问题：`5173` 代理打到 `8080`，但当前 `8080` 上跑的并不是这个工作区的后端进程。接下来我会把当前仓库后端拉起来并验证注册，再把代码里会导致 500 的入口补齐校验，确保这个接口稳定。

## 助手 · 2026-04-30T05:13:01.360Z

我已经确认到冲突源：你现在访问到的是另一个同名项目在占用 `5173/8080`。我先不动你的其它项目，直接把当前这个仓库启动到独立端口并验证注册，确保先恢复可用。

## 助手 · 2026-04-30T05:14:00.846Z

下一步我会在这个仓库单独启动后端（`18080`）做纯后端自测，确认注册接口本身是否正常；如果正常，我们就把你当前访问链路切回这个实例，500 会立即消失。

## 助手 · 2026-04-30T05:14:35.907Z

我先在当前这个仓库起一个隔离后端端口 `18081` 做纯净复现，这能百分百排除你电脑里“另一个同名项目进程”的干扰。复现完我会直接给你可用结论和修复。

## 助手 · 2026-04-30T05:15:54.413Z

我准备直接改后端 `register` 的参数校验：在入库前拦截 `account/password` 为空、密码太短等非法输入，避免再掉到数据库异常导致 500。改完我会立刻用真实请求回归验证。

## 助手 · 2026-04-30T05:16:14.775Z

参数校验已经加上了。现在我会重启隔离实例并做 4 组回归（正常注册、缺少 account、空字符串 account、短密码），确认从 500 变成可预期的业务错误。

## 助手 · 2026-04-30T05:16:47.312Z

我这边已经把 500 的直接代码风险修掉了，最后再给你确认一下改动文件和怎么在你机器上马上恢复正常访问链路。

## 助手 · 2026-04-30T05:17:11.214Z

你这个报错我已经定位并修掉了：`register` 在后端没有做参数兜底，`account` 缺失/为 `null` 时会直接打到数据库约束，返回 500。  
我已经在 [UserServiceImpl.java](D:\Java\class\projectKu\web\back\src\main\java\com\web\service\impl\UserServiceImpl.java) 加了注册和登录的参数校验与标准化处理（trim + 非空 + 长度 + 密码长度），现在这类输入会返回 `VALIDATION_FAILED`，不再是 500。

另外我还确认了你当前环境有**端口冲突/项目冲突**：  
`localhost:5173` 和 `8080` 上跑的是另一个目录 `D:\Java\class\projectKu\wenxuan\projectku-web` 的前后端进程，不是你现在这个 `D:\Java\class\projectKu\web`。这就是你在页面上一直看到 500 的主要外因。

建议你现在这样启动当前项目再测一次：
1. 先关掉占用 `5173/8080` 的旧进程（`wenxuan\projectku-web` 那套）。
2. 在 `D:\Java\class\projectKu\web\back` 启动后端：`mvn spring-boot:run`
3. 在 `D:\Java\class\projectKu\web\frontend` 启动前端：`npm run dev`
4. 再测 `POST http://localhost:5173/api/v1/auth/register`

我本地用隔离端口回归过了：
- 正常注册：200
- 缺少 `account`：400 + `VALIDATION_FAILED`
- 空 `account`：400 + `VALIDATION_FAILED`
- 短密码：400 + `VALIDATION_FAILED`

如果你愿意，我下一步可以直接帮你把这两个冲突端口的旧进程精确停掉，然后把当前仓库按标准端口拉起来。

## 用户 · 2026-04-30T05:35:25.140Z

http://localhost:5174/api/v1/admin/auth/login使用默认管理员账密无法登录400 Bad Request

## 助手 · 2026-04-30T05:35:42.484Z

这个我直接接着排查并修。先看 `admin/login` 的后端校验逻辑和默认管理员初始化 SQL，确认是密码不一致、账号状态，还是接口参数问题导致的 400。

## 助手 · 2026-04-30T05:36:43.749Z

我已经定位到根因了：当前库里 `admin@example.com` 的密码是 **BCrypt**（`$2a$...`），但登录代码只按 **MD5** 校验，所以默认 `admin123` 一定会被判错。  
我现在直接改后端登录逻辑为“同时兼容 BCrypt/MD5”，改完马上帮你实测 `5174` 登录。

## 助手 · 2026-04-30T05:36:49.464Z

我马上改 `UserServiceImpl`：优先识别 BCrypt 哈希（`$2a$/$2b$/$2y$`）用 BCrypt 校验，其他仍走 MD5，保证兼容历史数据。随后我会重启后端并直接验 `admin@example.com/admin123`。

## 助手 · 2026-04-30T05:37:30.002Z

后端已经按新逻辑重启中，我马上做管理员登录实测（走你现在的 `5174 -> /api -> 8080` 这条链路），确认 400 已消失。

## 助手 · 2026-04-30T05:37:41.215Z

问题已经修好，根因是密码算法不一致。

你库里 `admin@example.com` 的密码是 **BCrypt**（形如 `$2a$...`），但后端登录之前只按 **MD5** 校验，所以默认账号 `admin@example.com / admin123` 一直 400。  
我已在 [UserServiceImpl.java](D:\Java\class\projectKu\web\back\src\main\java\com\web\service\impl\UserServiceImpl.java) 改成兼容校验：

- 如果数据库密码是 `$2a$/$2b$/$2y$` 开头：走 BCrypt 校验
- 否则：继续走 MD5 校验（兼容旧数据）

并且我已实际验证两条链路都通过：
- `POST http://127.0.0.1:8080/api/v1/admin/auth/login` 成功
- `POST http://localhost:5174/api/v1/admin/auth/login`（你前端实际路径）成功

现在你刷新 `http://localhost:5174` 直接用默认管理员登录即可。  
如果页面还显示旧错误，清一下浏览器缓存或强刷（Ctrl+F5）再试。

## 用户 · 2026-04-30T05:48:23.628Z

服务器内部错误: Handler dispatch failed: java.lang.Error: Unresolved compilation problem: The method getCurrentAccount() is undefined for the type AuthInterceptor

## 助手 · 2026-04-30T05:48:34.381Z

我来直接修这个编译错误。先定位哪些地方调用了 `AuthInterceptor.getCurrentAccount()`，然后在拦截器里补上对应方法并做一次后端编译验证。

## 助手 · 2026-04-30T05:48:45.506Z

我会在 `AuthInterceptor` 里补全 `currentAccount` 的 ThreadLocal 存取，并在 `preHandle/afterCompletion` 同步设置与清理，这样 `AdminOperationLogAspect` 的调用就不会再报编译错误。随后我会编译验证。

## 助手 · 2026-04-30T05:49:05.336Z

已修复这个编译错误。

问题点是 `AdminOperationLogAspect` 调用了 `AuthInterceptor.getCurrentAccount()`，但 `AuthInterceptor` 里确实没有这个方法。  
我在 [AuthInterceptor.java](D:\Java\class\projectKu\web\back\src\main\java\com\web\interceptor\AuthInterceptor.java) 做了完整补齐：

- 新增 `CURRENT_ACCOUNT` 的 `ThreadLocal`
- `preHandle` 从 JWT 的 `account` payload 写入 `CURRENT_ACCOUNT`
- `afterCompletion` 清理 `CURRENT_ACCOUNT`
- 新增 `getCurrentAccount()` 静态方法

并且我已执行 `mvn -DskipTests compile`，编译通过（`BUILD SUCCESS`）。

你现在重启后端后再试一次该接口，`Unresolved compilation problem` 这个 500 会消失。

